"use client"

export default function Home() {
  return (
    <main>
        <button onClick={() => {
          // fetch data from api
        }}>
          Submit
        </button>
    </main>
    
  );
}
